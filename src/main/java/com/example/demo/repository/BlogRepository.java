// package com.example.demo.repository;

// import java.util.List;

// import org.springframework.data.jpa.repository.JpaRepository;

// import com.example.demo.model.Blog;

// public interface BlogRepository extends JpaRepository<Blog, Long> {
// //Rather than a complex Query Command we can implement query method
//     List<Blog> findByAuthorName(String authorName); // Find blogs by author's name
//     List<Blog> findByTitleContaining(String keyword); // Find blogs with a title containing a keyword
    
// }
